'use client'
import React, { ReactNode } from 'react'

type Props = { className?: string; children?: ReactNode; resetSignal?: boolean }

const MotDePasseConfirmation = (props: Props) => {
  const { className, children, resetSignal } = props

  return <div>MotDePasseConfirmation</div>
}

export default MotDePasseConfirmation
