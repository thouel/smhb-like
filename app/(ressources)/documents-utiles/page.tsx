import React from 'react'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Documents utiles - Saint-Médard Handball',
}
type Props = {}

const page = (props: Props) => {
  return <div>Documents utiles</div>
}

export default page
