import React from 'react'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Galerie photo - Saint-Médard Handball',
}
type Props = {}

const page = (props: Props) => {
  return <div>Galerie photo</div>
}

export default page
