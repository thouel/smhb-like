import React from 'react'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Mentions légales - Saint-Médard Handball',
}
type Props = {}

const page = (props: Props) => {
  return <div>Mentions l&eacute;gales</div>
}

export default page
