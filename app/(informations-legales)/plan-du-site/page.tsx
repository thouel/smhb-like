import React from 'react'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Plan du site - Saint-Médard Handball',
}
type Props = {}

const page = (props: Props) => {
  return <div>Plan du site</div>
}

export default page
