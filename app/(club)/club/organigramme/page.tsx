import React from 'react'

import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Notre organisation - Saint-Médard Handball',
}
type Props = {}

const page = (props: Props) => {
  return <div>L&apos;organigramme</div>
}

export default page
